{
  "name": "open-constructor-prototype",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "test": "npm run build && node build/unit-test/index.js",
    "decoder": "npm run build && node build/decodeSave.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.1",
    "typescript": "^5.1.6"
  },
  "engines" : {
    "node" : ">=18.0.0"
  }
}
